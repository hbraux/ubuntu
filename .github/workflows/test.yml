name: test
on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2      
    - name: test install.sh
      run : |
        ./install.sh default
